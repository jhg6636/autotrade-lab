# LUNA-101-01 — academic discovery batch

- Base integration branch: `integration/LUNA-101-academic-discovery`
- Batch branch: `agent/LUNA-101-01-academic-batch`
- Starting commit: `3f74f7d`
- Collection scope: eight new unique public NBER academic/replication/robustness candidates.
- No canonical promotion, strategy code, optimization, backtest, order, Toss API, ranking, or evidence score.

## Public-source verification

The eight added URLs were checked against their public NBER landing pages and available public
NBER Reporter/PDF links. The batch remains a flat pool containing positive, negative, theoretical,
and robustness/risk findings.

- w20660: public page describes mechanical momentum and crash conditions, but not complete trigger,
  rebalance, and sizing rules — `incomplete`.
- w5375: public page describes return/earnings-surprise predictability, but not exact cutoffs,
  holding, or weights — `incomplete`.
- w24748: public page describes announcement drift, but exact surprise construction, timestamps,
  exit, and sizing are not fully public in the checked material — `incomplete`.
- w7835: title corrected to **Rebels, Conformists, Contrarians and Momentum Traders**; theoretical
  alternatives do not provide an operational rule — `incomplete`.
- w14500: historical momentum/reversal findings lack complete portfolio construction and execution
  details — `incomplete`.
- w6553: size/analyst-coverage conditional findings lack exact formation, holding, and weighting
  rules — `incomplete`.
- w16942: the public NBER Reporter description states a monthly prior-month-return currency
  momentum variant over 20 currencies, long/short halves, equal-weighted. That exact stated
  variant is recorded as `usable`; currency execution remains `context_only` — `usable`.
- w18169: tail-risk and leverage-dynamics robustness findings lack a complete portfolio recipe —
  `incomplete`.

No rule was inferred from titles, charts, claimed performance, or theoretical discussion. The
previous usable arXiv Scenario 1 ADF candidate remains unchanged.

## Counts and shortfall

After the batch, collection totals are 13 sources and 13 hypotheses: 2 usable and 11 incomplete
in each ledger. The target remains 80 usable sources and 30 usable hypotheses/variants. The batch
truthfully falls short of its aspirational eight usable-source/five usable-hypothesis goal because
seven records did not expose complete falsifiable rules in the checked public material. No record
was promoted to satisfy a count.

## Validation

```text
.venv/bin/python research/aggregate_staging.py --staging-dir research/staging/collection/luna-101 --report research/runs/LUNA-101-dry-run.json
.venv/bin/python research/aggregate_staging.py --staging-dir research/staging/collection/luna-101 --report /tmp/luna101-dry-b.json
.venv/bin/pytest -q
.venv/bin/ruff check .
.venv/bin/ruff format --check .
git diff --check
```

The two aggregate reports were byte-identical. The report retains `automatic_merge=false` and
`canonical_records_modified=false`; no files under `research/strategies/` or the strategy index
were changed. Stop conditions remain: inaccessible or underspecified sources stay incomplete or
inaccessible; only a system-level blocker stops the bounded batch.
