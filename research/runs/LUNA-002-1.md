# LUNA-002-1 run report

- Scope: follow-up review corrections on the existing LUNA-002 branch; no new strategies or sources added.
- Canonical flow: `research/strategies/*.json` → validator and integrity checks → `research/build_strategy_index.py` → `research/strategy_index.csv`.
- Migration boundary: `research/migrate_catalog.py` remains a one-time bootstrap from the legacy Python catalog and refuses to overwrite canonical records by default.
- Validation added: application directions and execution scopes must reconcile with their top-level summaries (`mixed` for multiple directions; `executable` if any application is executable).
- Integrity checks added: committed JSON schema validity, filename/ID identity, unique IDs and canonical names, implementation path/symbol resolution, CSV parity, and byte-for-byte deterministic regeneration.
- Commands run: `.venv/bin/pytest -q`; `.venv/bin/ruff check .`; `.venv/bin/ruff format --check .`; `git diff --check`; canonical JSON→CSV regeneration with byte-for-byte parity.
- Results: 31 passed; Ruff check clean; Ruff format check clean; diff check clean; regenerated CSV matches the committed index byte-for-byte.
- Remaining limitation: implementation coverage remains structural metadata and is not profitability or live-trading evidence.
