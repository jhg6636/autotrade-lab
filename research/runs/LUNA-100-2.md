# LUNA-100-2-4 — validator and report repair

- Base commit: `9c21c21efbb0173a54c902f7142a2d2aa8af62d5`
- Branch: `agent/LUNA-100-2-4-validator-report-repair`
- Implementation commits: `2a3bef0` (validator/report repair) and `bdb9d34` (pilot files restored)
- Collection/access review date: 2026-08-21
- The prior pilot was reviewed and corrected; no canonical strategy JSON or strategy index was modified.

## Public-source review scope

The academic records were checked against their public landing-page metadata. SSRN 166840 now records Narasimhan Jegadeesh and Sheridan Titman with 1999-07-21; NBER w8734 records the 2002-01 issue; NBER w16747 records Laura Xiaolei Liu and Lu Zhang with 2011-01; NBER w7032 records the 1999-03 issue; and arXiv 2109.10662 records Masood Tadi and Irina Kortchmeski.

The arXiv paper was read beyond its abstract. To avoid mixing the paper's three scenarios, the pilot records only Scenario 1 ADF variant: OLS spread `S=P1-beta*P2-alpha`, Engle-Granger/ADF 1% filtering, OU `lambda=theta_hat`, `N_SMA=2/lambda-1`, signed two-bar crossings for long/short entry and exit, three-month formation and one-week trading, integerized coefficients, and approximately 1 XBT weekly capital. Best bid/ask execution is retained as context-only. The candidate is `usable` under the staging schema, with fees and residual rounding explicitly ambiguous and its result labelled `unverified`. `long_short` is used rather than claiming guaranteed delta neutrality because the source documents paired futures positions but does not establish a zero-net-beta invariant. It remains `context_only` because BitMEX perpetual/futures execution is outside the Korean Toss scope.

The NBER w16747 description was corrected to an investment-based model involving expected growth and marginal productivity, not gradual information diffusion.

The Korean lane has five unique source URLs. The former duplicate KRX settlement URL was replaced by the public KRX ETF listing page, whose relevant scope is ETF delisting: correlation below 0.9 between ETF per-unit NAV daily returns and underlying-index daily returns for three months. The source and candidate now use NAV daily returns, index daily returns, and the three-month correlation as required inputs. The loader rejects duplicate exact source URLs even when source IDs differ, while still allowing multiple hypotheses to reference the same source; this behavior is covered by an aggregate-boundary regression test.

KCMI metadata was corrected to 2022-02-03 for the behavior study and 2026-02-04 for the asset-management issues publication; official institutional/author metadata is preserved in each record.

## Status accounting and reasons

- Academic: 5 sources / 5 hypotheses. Four source-candidate pairs remain `incomplete` because public landing pages do not expose falsifiable thresholds, exits, and sizing. The arXiv pair is `usable` at staging level; its execution remains context-only.
- Korean: 5 sources / 5 hypotheses. All remain `incomplete`: the official KRX/KCMI pages are useful market/product context but do not state complete automated entry, exit, and sizing rules. No Korean candidate was promoted to executable.
- Unique source URL count: 10 across both lanes; 5 per lane.
- No Korean-equity short-selling rule was made executable, and no Korean live order, backtest, automatic merge, or canonical promotion was performed. The crypto Scenario 1 candidate retains its documented long/short context rules.

## Validation

Commands:

```text
.venv/bin/pytest -q
.venv/bin/ruff check .
.venv/bin/ruff format --check .
git diff --check
.venv/bin/python research/aggregate_staging.py --staging-dir research/staging/pilot/luna-101 --report /tmp/luna101-a.json
.venv/bin/python research/aggregate_staging.py --staging-dir research/staging/pilot/luna-104 --report /tmp/luna104-a.json
```

Both lane reports are deterministic on repeated execution and retain `automatic_merge=false` and `canonical_records_modified=false`. `tests/test_staging.py::test_duplicate_source_urls_are_rejected_at_aggregate_boundary` verifies URL uniqueness, and the public pilot signal/KRX test remains ID-based. Canonical strategy JSON/index files are unchanged.
