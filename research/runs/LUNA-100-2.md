# LUNA-100-2-1 — source pilot corrections

- Base commit: `9c21c21efbb0173a54c902f7142a2d2aa8af62d5`
- Branch: `agent/LUNA-100-2-1-source-pilot-corrections`
- Collection/access review date: 2026-08-21
- The prior pilot was reviewed and corrected; no canonical strategy JSON or strategy index was modified.

## Public-source review scope

The academic records were checked against their public landing-page metadata. SSRN 166840 now records Narasimhan Jegadeesh and Sheridan Titman with 1999-07-21; NBER w8734 records the 2002-01 issue; NBER w16747 records Laura Xiaolei Liu and Lu Zhang with 2011-01; NBER w7032 records the 1999-03 issue; and arXiv 2109.10662 records Masood Tadi and Irina Kortchmeski.

The arXiv paper was read beyond its abstract. Its candidate now records BitMEX futures/perpetuals, minute-binned data, weekly formation/trading, OLS/cointegration/half-life pair selection, 2-standard-deviation Z-score entry, 1-standard-deviation reversion exit, and integerized pair weights. It is `usable` under the staging schema, with unresolved implementation conventions explicitly listed as ambiguities and its claimed result labelled `unverified`. It remains `context_only` because BitMEX perpetual/futures execution is outside the Korean Toss scope.

The NBER w16747 description was corrected to an investment-based model involving expected growth and marginal productivity, not gradual information diffusion.

The Korean lane has five unique source URLs. The former duplicate KRX settlement URL was replaced by the public KRX ETF listing/measurement requirements page. The loader now rejects duplicate source URLs while still allowing multiple hypotheses to reference the same source.

KCMI metadata was corrected to 2022-02-03 for the behavior study and 2026-02-04 for the asset-management issues publication; official institutional/author metadata is preserved in each record.

## Status accounting and reasons

- Academic: 5 sources / 5 hypotheses. Four source-candidate pairs remain `incomplete` because public landing pages do not expose falsifiable thresholds, exits, and sizing. The arXiv pair is `usable` at staging level; its execution remains context-only.
- Korean: 5 sources / 5 hypotheses. All remain `incomplete`: the official KRX/KCMI pages are useful market/product context but do not state complete automated entry, exit, and sizing rules. No Korean candidate was promoted to executable.
- Unique source URL count: 10 across both lanes; 5 per lane.
- No short rule, live order, backtest, automatic merge, or canonical promotion was performed.

## Validation

Commands:

```text
.venv/bin/pytest -q
.venv/bin/ruff check .
.venv/bin/ruff format --check .
git diff --check
.venv/bin/python research/aggregate_staging.py --staging-dir research/staging/pilot/luna-101 --report /tmp/luna101-a.json
.venv/bin/python research/aggregate_staging.py --staging-dir research/staging/pilot/luna-104 --report /tmp/luna104-a.json
```

Both lane reports are deterministic on repeated execution and retain `automatic_merge=false` and `canonical_records_modified=false`. The URL uniqueness regression is covered by `tests/test_staging.py`.
