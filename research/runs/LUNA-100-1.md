# LUNA-100-1 run report

- Starting commit: `c32a69b` (LUNA-100 collection substrate)
- Finished commit: implementation commit containing this report
- Started/finished at (KST): 2026-08-21 / 2026-08-21
- Inputs inspected: staging schemas/loader, aggregate CLI, LUNA-101/104 synthetic fixtures, existing discovery tests
- Sources added: none; no web collection or source research performed
- Files changed: status/completion aggregation, application-summary and executable-Korean validation, required-file checks, recursive placeholder/claim checks, regression tests, and dry-run report
- Commands run: `.venv/bin/pytest -q`; `.venv/bin/ruff check .`; `.venv/bin/ruff format --check .`; `git diff --check`; deterministic `research/aggregate_staging.py` reruns
- Test results: pending final verification
- Completion conditions: total/usable/incomplete/inaccessible counts; usable 80/30 target counters; required sources/hypotheses files; optional auxiliary files; top-level/application summary consistency; executable Korean application timeframe and exposure rules; recursive placeholder checks; every usable claimed result explicitly unverified
- Assumptions: records with no `status` in optional relation/audit files are counted under `other`; only actual executable Korean applications constrain timeframe; aggregate remains dry-run and canonical records are never mutated
- Ambiguities preserved: synthetic fixtures are shape tests only and do not count toward discovery targets
- Blockers: GitHub push/Draft PR creation may require valid repository authentication/network access
- Recommended next task: review hardening contract before actual LUNA-101/104 collection
